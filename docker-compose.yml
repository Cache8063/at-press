services:
  blog:
    build: .
    container_name: at-press
    restart: unless-stopped
    ports:
      - "0.0.0.0:4000:4000"
    env_file: .env
    environment:
      - HOST=0.0.0.0
      - PORT=4000
      - DRAFTS_DB_PATH=/data/drafts.db
    volumes:
      - blog-data:/data

volumes:
  blog-data:
